const Dropdown = () => {
  return {
    /* <div className="flex flex-col items-start h-100 ">
          <button onClick={() => setMode(!mode)}>
            Bloqlar
            <i className="ml-1 fa fa-caret-down" aria-hidden="true"></i>
          </button>
          <div
            className={` text-black px-4 py-3 text-[15px] rounded  
              absolute mt-8 z-1 bg-[#fff]  flex-col items-start gap-2 ${
                !mode ? "flex" : "hidden"
              }`}
          >
            <Link to="/">Bloq yaz</Link>
            <Link to="/">Bloq oxu</Link>
          </div>
        </div> */
  };
};

export default Dropdown;
